using fitnesse.mtee.application;
using NUnit.Framework;

namespace fitnesse.unitTest.application {
/*    [TestFixture] public class ContextTest {
        [Test] public void ContextIsSavedAndRestored() {
            Context.Current.GetItem<TestItem>().Data = "stuff";
            Assert.AreEqual("stuff", Context.Current.GetItem<TestItem>().Data);
            Context.Save();
            Assert.AreEqual("stuff", Context.Current.GetItem<TestItem>().Data);
            Context.Current.GetItem<TestItem>().Data = "nonsense";
            Assert.AreEqual("nonsense", Context.Current.GetItem<TestItem>().Data);
            Context.Restore();
            Assert.AreEqual("stuff", Context.Current.GetItem<TestItem>().Data);
        }

        private class TestItem {
            public string Data;
        }
    }*/
}
